/*  Legge il file Highway.txt, prende in input un filename
    carica le indicazioni da questo file, crea due strutture dati (vettori, liste ordinate, altro)
    e crea un oggetto Highway, restituendo un puntatore ad esso

    es.

    load_highway(string filename){
        //leggi file
        for(riga in file){
            if(riga is Varco) v.add(riga)
            if(riga is Svincolo) s.add(riga)
            else throw error
        }
        
        return new Highway(v,s);
    }

*/